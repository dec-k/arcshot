{
  "name": "arcshot",
  "version": "0.2.0",
  "private": true,
  "description": "Browser extension that adds backgrounds and frames to screenshots.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
